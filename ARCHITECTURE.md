# Dashboard Architecture Overview

## 🏗️ Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         DASHBOARD FEATURE                        │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                      PRESENTATION LAYER                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │         DashboardScreen (Main Screen)                     │  │
│  │  - Weekly/Monthly Toggle                                 │  │
│  │  - API/Mock Data Toggle                                  │  │
│  │  - Pull-to-Refresh                                       │  │
│  │  - Error Handling & Loading States                       │  │
│  └──────────────────────────────────────────────────────────┘  │
│                              │                                   │
│              ┌───────────────┼───────────────┬─────────────┐   │
│              │               │               │             │   │
│         ┌────▼────┐    ┌────▼────┐    ┌────▼────┐   ┌───▼──┐ │
│         │StatCard │    │Line     │    │Bar      │   │Pie   │ │
│         │Widget   │    │Chart    │    │Chart    │   │Chart │ │
│         └─────────┘    └─────────┘    └─────────┘   └──────┘ │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
                              │
                              │ Uses
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                         DATA LAYER                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌────────────────────────┐      ┌─────────────────────────┐   │
│  │  DashboardApiService   │      │  MockDashboardData      │   │
│  │  - getStats()          │      │  - getStats()           │   │
│  │  - getWeeklyData()     │      │  - getWeeklyData()      │   │
│  │  - getMonthlyData()    │      │  - getMonthlyData()     │   │
│  │  - getActivityBrkdown()│      │  - getActivityBrkdown() │   │
│  │  - getWeeklySummary()  │      │  - getWeeklySummary()   │   │
│  │  - getMonthlySummary() │      │  - getMonthlySummary()  │   │
│  └────────────────────────┘      └─────────────────────────┘   │
│             │                                  │                 │
│             │ HTTP Calls                       │ In-Memory      │
│             ▼                                  ▼                 │
│  ┌────────────────────┐            ┌─────────────────────┐     │
│  │ NestJS Backend API │            │  Static Mock Data   │     │
│  │ localhost:3001     │            │  (Development)      │     │
│  └────────────────────┘            └─────────────────────┘     │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
                              │
                              │ Returns
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                        DOMAIN LAYER                              │
├─────────────────────────────────────────────────────────────────┤
│                         ENTITIES                                 │
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐     │
│  │ Dashboard    │  │  WeeklyData  │  │  MonthlyData     │     │
│  │ Stats        │  │              │  │                  │     │
│  └──────────────┘  └──────────────┘  └──────────────────┘     │
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐                            │
│  │ Activity     │  │  Dashboard   │                            │
│  │ Breakdown    │  │  Summary     │                            │
│  └──────────────┘  └──────────────┘                            │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

## 📊 Data Flow

```
User Action (Pull-to-Refresh / Toggle View)
           │
           ▼
    DashboardScreen
           │
           ├─── [Mock Mode] ──────► MockDashboardData
           │                              │
           │                              ▼
           │                        Static Data
           │                              │
           └─── [API Mode] ───────► DashboardApiService
                                          │
                                          ▼
                                    HTTP Request
                                          │
                                          ▼
                                    NestJS Backend
                                          │
                                          ▼
                                    JSON Response
           ┌──────────────────────────────┘
           │
           ▼
    Parse to Domain Models
           │
           ▼
    Update UI State
           │
           ▼
    Render Widgets (Cards, Charts)
```

## 🔄 Component Interaction

```
┌─────────────────────────────────────────────────────────┐
│                   User Interface                         │
└─────────────────────────────────────────────────────────┘
                         │
        ┌────────────────┼────────────────┐
        │                │                │
        ▼                ▼                ▼
   ┌────────┐      ┌─────────┐     ┌──────────┐
   │ Toggle │      │ Refresh │     │  Tap on  │
   │  View  │      │  Data   │     │  Chart   │
   └────────┘      └─────────┘     └──────────┘
        │                │                │
        └────────────────┼────────────────┘
                         ▼
                ┌────────────────┐
                │   setState()   │
                └────────────────┘
                         │
                         ▼
                ┌────────────────┐
                │  Rebuild UI    │
                └────────────────┘
```

## 🎨 Widget Hierarchy

```
Scaffold
 │
 ├── AppBar
 │    ├── Title: "Dashboard"
 │    ├── Cloud Icon (Data Source Toggle)
 │    └── Refresh Icon
 │
 └── Body: RefreshIndicator
      │
      └── SingleChildScrollView
           │
           ├── View Toggle (Weekly/Monthly)
           │
           ├── GridView (2x2)
           │    ├── StatCard (Workouts)
           │    ├── StatCard (Calories)
           │    ├── StatCard (Distance)
           │    └── StatCard (Duration)
           │
           ├── CaloriesLineChart
           │    └── FL Chart (Line)
           │
           ├── WorkoutsBarChart
           │    └── FL Chart (Bar)
           │
           └── ActivityDoughnutChart
                └── FL Chart (Pie)
```

## 🗂️ Module Organization

```
dashboard/
│
├── dashboard_module.dart          [Exports all components]
│
├── domain/                        [Business Logic]
│   └── entities/                  [Data Models]
│       ├── dashboard_stats.dart
│       ├── weekly_data.dart
│       ├── monthly_data.dart
│       ├── activity_breakdown.dart
│       └── dashboard_summary.dart
│
├── data/                          [Data Access]
│   └── dashboard_api_service.dart [HTTP Service]
│
├── mocks/                         [Test Data]
│   └── mock_dashboard_data.dart   [Static Mock]
│
└── presentation/                  [UI Components]
    ├── screens/
    │   └── dashboard_screen.dart  [Main Screen]
    └── widgets/
        ├── stat_card.dart
        ├── calories_line_chart.dart
        ├── workouts_bar_chart.dart
        └── activity_doughnut_chart.dart
```

## 🔌 API Integration Points

```
┌──────────────────────────────────────────────────────────┐
│                  Frontend (Flutter)                       │
├──────────────────────────────────────────────────────────┤
│                                                           │
│  DashboardApiService                                     │
│                                                           │
│  ┌─────────────────────┐                                │
│  │ getStats()          │ ─────► GET /api/dashboard/stats│
│  ├─────────────────────┤                                │
│  │ getWeeklyData()     │ ─────► GET /api/dashboard/weekly
│  ├─────────────────────┤                                │
│  │ getMonthlyData()    │ ─────► GET /api/dashboard/monthly
│  ├─────────────────────┤                                │
│  │ getActivityBrkdown()│ ─────► GET /api/dashboard/activity-breakdown
│  ├─────────────────────┤                                │
│  │ getWeeklySummary()  │ ─────► GET /api/dashboard/summary/weekly
│  ├─────────────────────┤                                │
│  │ getMonthlySummary() │ ─────► GET /api/dashboard/summary/monthly
│  └─────────────────────┘                                │
│                                                           │
└──────────────────────────────────────────────────────────┘
                           │
                           │ HTTP/JSON
                           ▼
┌──────────────────────────────────────────────────────────┐
│                  Backend (NestJS)                         │
├──────────────────────────────────────────────────────────┤
│                                                           │
│  DashboardController                                     │
│                                                           │
│  @Get('stats')                                           │
│  @Get('weekly')                                          │
│  @Get('monthly')                                         │
│  @Get('activity-breakdown')                              │
│  @Get('summary/weekly')                                  │
│  @Get('summary/monthly')                                 │
│                                                           │
└──────────────────────────────────────────────────────────┘
```

## 🎯 State Management

```
┌─────────────────────────────────────────────┐
│          DashboardScreen State              │
├─────────────────────────────────────────────┤
│                                             │
│  bool _isLoading = true                    │
│  bool _useMockData = true                  │
│  String? _error = null                     │
│  DashboardView _currentView = weekly       │
│                                             │
│  DashboardStats? _stats                    │
│  List<WeeklyData> _weeklyData              │
│  List<MonthlyData> _monthlyData            │
│  List<ActivityBreakdown> _activityBrkdown  │
│                                             │
└─────────────────────────────────────────────┘
                    │
                    │ setState()
                    ▼
              Widget Rebuild
```

## 📈 Chart Data Pipeline

```
Raw Data (API/Mock)
      │
      ▼
Domain Models
      │
      ├─► WeeklyData[] ──┐
      │                   │
      └─► MonthlyData[]───┤
                          │
                          ▼
                   Extract Values
                          │
                ┌─────────┼─────────┐
                │         │         │
                ▼         ▼         ▼
           calories   workouts   labels
                │         │         │
                └─────────┼─────────┘
                          │
                          ▼
                    Chart Widgets
                          │
                ┌─────────┼─────────┐
                │         │         │
                ▼         ▼         ▼
          LineChart  BarChart  PieChart
                │         │         │
                └─────────┼─────────┘
                          │
                          ▼
                   Visual Display
```

## 🔐 Error Handling Flow

```
API Call
   │
   ├─► Success ──────────────► Update State ─► Show Data
   │
   ├─► Network Error ────────► Show Error Message ─► Retry Button
   │
   ├─► 404 Not Found ────────► Show "No Data" ─► Refresh Option
   │
   └─► 500 Server Error ─────► Show Error ─► Switch to Mock Data
```

## 🚀 Navigation Integration

```
App Entry Point (main.dart)
         │
         ▼
    MaterialApp
         │
         ▼
   AppRouter.splash
         │
         ▼
   SplashScreen
         │
         ▼
    AppShell (Bottom Nav)
         │
    ┌────┼────┬────┬────┬────┐
    │    │    │    │    │    │
    ▼    ▼    ▼    ▼    ▼    ▼
  Dash Goals Today Forum Profile
  board
```

---

**Architecture implemented following Clean Architecture principles! 🏗️**
**Separation of concerns maintained throughout! ✨**
